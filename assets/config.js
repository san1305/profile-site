/* ============================================================
   Site configuration — EDIT THESE TWO VALUES.
   ============================================================
   1. calendlyUrl  → paste your Calendly (or Cal.com) scheduling link.
                     Get it from Calendly → "Share" → "Copy link".
                     Example: "https://calendly.com/sanu-dutta/30min"
   2. chatEndpoint → leave as "/api/chat" when hosted on Vercel/Netlify.
   ============================================================ */
window.SANU_CONFIG = {
  calendlyUrl: "https://calendly.com/sanu-dutta",
  chatEndpoint: "/api/chat"
};
